# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
myvenv/
env/
.env
.env.local
.env.*.local
# env backups taken during the Supabase migration (contain live keys)
*.old-personal
# TEMPORARY: backend/.env.example currently holds REAL keys (service-role,
# OpenAI, AssemblyAI). Excluded so they don't leak into git history.
# Once it's sanitised to placeholders, delete this line and commit it.
.env.example

# Node
node_modules/
.next/
out/
.npm
.eslintcache

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Supabase local
.branches/
.temp/
